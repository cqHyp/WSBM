<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="BottomTab" parent="Widget.Design.TabLayout" >
        <item name="tabIndicatorColor">@android:color/white</item>
        <item name="tabTextColor">@color/select_no</item>
        <item name="tabSelectedTextColor">@color/select_no</item>
        <item name="tabBackground">@drawable/bg_button_normal</item>
        <item name="tabIndicatorHeight">0dp</item>
    </style>

    <style name="NewsTab" parent="Widget.Design.TabLayout" >
        <item name="tabIndicatorColor">@color/mainColor</item>
        <item name="tabTextColor">@color/select_no</item>
        <item name="tabSelectedTextColor">@color/mainColor</item>
        <item name="tabBackground">@drawable/bg_button_normal</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >
        <item name="colorPrimary">@color/mainColor</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" >
        <item name="colorPrimary">@color/mainColor</item>
    </style>

    <style name="mProgress_horizontal">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/progressbar_horizontal_lv</item><!-- progress_horizontal -->
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:minHeight">20dip</item>
        <item name="android:maxHeight">20dip</item>
    </style>

</resources>
